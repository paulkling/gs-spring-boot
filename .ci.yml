environment:
  language: java
  language_versions: 1.6.0_24
  
build:
  info:
    - ls -la
    - echo "Env:" ; env
    - java -version
    - git clone https://github.com/paulkling/gs-spring-boot.git
  run: gradlew build 

